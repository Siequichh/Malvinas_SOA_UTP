spring:
  config:
    activate:
      on-profile: dev

server:
  port: 8080

routes:
  auth-uri: http://localhost:8086
  personal-uri: http://localhost:8081
  vehiculos-uri: http://localhost:8082
  cargas-uri: http://localhost:8083
  rutas-uri: http://localhost:8084
  reportes-uri: http://localhost:8085

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 30

jwt:
  secret: malvinas-dev-secret-key-change-in-production

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true

logging:
  level:
    com.malvinas: DEBUG
    org.springframework.cloud.gateway: DEBUG
    org.springframework.cloud.loadbalancer: DEBUG
    org.springframework.boot.autoconfigure.security: ERROR
